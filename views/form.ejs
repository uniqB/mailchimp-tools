<!DOCTYPE html>
<html>
<meta charset="utf-8">
<meta http-equiv="X-UA-Compatible" content="IE=edge">
<meta name="viewport" content="width=device-width, initial-scale=1">

<!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
<title>Dong Hanh Technology</title>
<link href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/3.4.0/css/bootstrap.min.css" rel="stylesheet">

<body>
  <div style="margin-top: 40px">
    <div class="col-md-4 col-md-offset-4 col-sm-8 col-sm-offset-2">
      <div class="panel panel-info">
        <div class="panel-heading">
          <div class="panel-title">Update status of all members in a MailChimp list</div>
        </div>
        <div class="panel-body">
          <div class="signup-container">
            <% if(status === 'success') { %>
              <div class="alert alert-success">
                <span>
                Success! Updated <%= count %> members!
                </span>
              </div>
              <% } %>

              <% if(status === 'error') { %>
                <div class="alert alert-danger">
                  <span>
                    Error! Check input fields and try again.
                  </span>
                </div>
                <% } %>
            <form action="" method="post" role="form">
              <div class="form-group">
                <label>API Keys</label>
                <input type="text" class="form-control" name="apiKey" value="<%= data.apiKey %>" placeholder="bf49c0cd6299a492fec1327a09c51xxx-us12">
              </div>
              <div class="form-group">
                <label>List ID</label>
                <input type="text" class="form-control" name="listId" value="<%= data.listId %>" placeholder="6f23348xxx">
              </div>

              <div class="form-group">
                <label>Status</label>
                <select class="form-control" name="status" value="<%= data.status %>" placeholder="Trạng thái">
                  <option value="subscribed">subscribed</option>
                  <option value="unsubscribed">unsubscribed</option>
                </select>
              </div>

              <div class="form-group">
                <label>Limit members</label>
                <input type="text" class="form-control" name="limit" value="<%= data.limit %>"  placeholder="100">
              </div>

              <div class="form-group">
                <button id="btn-login" type="submit" class="btn btn-primary btn-sm">Submit</button>
              </div>

            </form>

          </div>
        </div>
      </div>
    </div>

  </div>

</body>


</html>